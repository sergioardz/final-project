import React from "react";
import "./hero.css";

const Hero = props => (
  <div className="jumbotron jumbotron-fluid">
    <div id="hero" className="container">
      <h4>Production Control Management System</h4>
      <p className="lead">Monitor Production Orders, their stats and KPIs</p>
    </div>
  </div>
);

export default Hero;
